pub const DOCUMENTS: [&str; 8] = [
    "serde_json is the standard crate for parsing and serializing JSON in Rust.",
    "Paris is the capital and largest city of France.",
    "Axum is a web application framework focused on ergonomics and modularity.",
    "A short document.",
    "Rust ownership enables memory safety without a garbage collector. Borrowing allows code to temporarily access values without taking ownership, while lifetimes let the compiler verify that references remain valid.",
    "CUDA executes parallel kernels on NVIDIA GPUs and provides optimized linear algebra through cuBLAS.",
    "Tokenization converts text into model input identifiers. Batching combines multiple padded sequences into one model invocation so matrix multiplication can process them together.",
    "SearXNG is a metasearch engine that aggregates results from multiple search services while giving operators control over privacy and configuration.",
];
