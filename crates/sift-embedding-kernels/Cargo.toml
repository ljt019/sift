[package]
name = "sift-embedding-kernels"
version = "0.1.0"
edition = "2021"
license = "MIT"
publish = false

[features]
default = []
cuda = ["dep:cudaforge"]
rocm = []

[build-dependencies]
cudaforge = { version = "0.1.2", optional = true }
